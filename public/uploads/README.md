# Uploads Directory

Place your MP4 video file here named `video.mp4` to be accessed at:
`/uploads/video.mp4`

When `CONFIG.mode = 'video'` is set in `scroll-video/js/app.js`, the scroll engine will automatically stream and scrub this video frame-by-frame based on the user's vertical scroll position.
